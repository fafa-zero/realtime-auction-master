{
  "name": "@realtime-auction/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "reset:demo": "node scripts/reset-demo-state.mjs",
    "test:integration": "npm run build && node scripts/integration-test.mjs",
    "test:agent-contract": "npm run build && node scripts/agent-contract-test.mjs",
    "test:state-machine": "npm run build && node scripts/state-machine-test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mysql2": "^3.15.3",
    "socket.io": "^4.8.1",
    "uuid": "^11.0.5",
    "ws": "^8.20.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
