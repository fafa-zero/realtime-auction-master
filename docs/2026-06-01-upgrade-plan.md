# 2026-06-01 演示稳定性与加分升级计划

## 目标

围绕最终演示和答辩，把项目从“功能可跑”升级为“状态可恢复、流程可复现、文档可解释、展示更像直播电商系统”的版本。

## 最高优先级

### 1. 稳定演示数据一键恢复

目标：避免账号、直播间、图片和竞拍状态被本地测试数据污染。

已完成：

- 主播端已有“重置演示数据”按钮。
- 后端 `/api/admin/reset-demo` 会恢复 `demo-host`、`demo-buyer`、`live-1`、`live-2`、标准商品、本地图片、竞拍状态、订单、弹幕、用户和会话。
- 标准商品图固定为 `/static/jewelry.jpg` 和 `/static/watch.jpg`，不依赖外网图片。

### 2. 演示流程固化

目标：答辩前用固定命令恢复稳定演示入口。

已完成：

- 新增根目录脚本 `npm run demo`。
- 脚本会先构建 Web 和后端，自动恢复标准演示数据，再用 `PORT=4300 CLIENT_URL=http://localhost:4300` 启动后端。
- 新增 `npm run demo:reset`，用于不重启服务时从命令行恢复标准演示数据。
- 标准演示账号、直播间、商品队列、本地图片和初始竞拍状态都可通过重置接口恢复。

建议演示启动流程：

```bash
cd /home/zyy/realtime-auction-master
npm install
npm run demo
```

打开：

- 主播端：`http://localhost:4300/host`
- Web 观众端：`http://localhost:4300/live/live-1`
- 健康检查：`http://localhost:4300/api/demo/check`

### 3. 小程序文档和实际能力对齐

目标：统一说明为“WebSocket 实时同步，REST 兜底”。

已完成：

- README、小程序 README、弹幕功能文档和测试用例已统一描述。
- 小程序默认尝试 `http://localhost:4300` 和 `http://127.0.0.1:4300`。
- 小程序详情页优先连接 `/miniprogram-ws`，REST 轮询和 HTTP 出价/弹幕接口作为兜底。

## 比较加分

### 4. 主播端数据看板

目标：让系统更像直播电商运营后台。

已完成：

- 主播端新增/调整看板指标：
  - 当前直播间观看人数
  - 出价次数
  - 参与人数
  - 成交率
  - GMV/成交金额
  - 待支付订单数

### 5. AI 能力业务化

当前能力：

- 商品讲解词。
- 竞拍复盘。
- 主播实时话术。
- 异常出价提示。

已完成：

- 新增 `POST /api/ai/host-cue`。
- 主播端 AI 竞拍助手新增“主播话术”按钮，可结合商品、当前价、最近出价和弹幕生成可直接念的引导话术。

后续可继续增强：

- 成交后自动生成复盘。
- 高频出价或接近封顶价时自动弹出主播提醒。
- 给买家侧生成商品亮点摘要。

### 6. 商品导入模板升级

目标：导入商品后直接展示图片。

已完成：

- `docs/product-import-template.csv` 新增“商品图片”列。
- 后端导入支持 `商品图片`、`图片`、`图片地址`、`imageUrl` 字段。
- 支持 `/static/jewelry.jpg`、`/static/watch.jpg` 或网络图片 URL。
- 未填写图片时会按商品名称和直播间标题自动兜底本地演示图。

## 工程质量升级

### 7. 统一端口和启动脚本

已完成：

- 根目录新增 `npm run demo`。
- 推荐演示统一使用 `http://localhost:4300`，避免 Vite 端口变化和 WSL 转发不稳定。

### 8. 健康检查接口

已完成：

- 新增 `GET /api/demo/check`。
- 检查内容包括：
  - API 是否正常。
  - 演示账号是否存在。
  - `demo-host` 是否拥有标准直播间。
  - 标准直播间是否使用本地图片。
  - 小程序 API 候选地址和 WebSocket 路径提示。

### 9. 自动化测试补前端冒烟

当前状态：

- 后端集成测试已覆盖权限、竞拍、支付、弹幕和隔离流程。

后续建议：

- 使用 Playwright 做最小前端冒烟：
  - 打开 `/host`。
  - 登录 `demo-host`。
  - 看到 `live-1` 和 `live-2`。
  - 商品图片加载成功。
  - 打开 `/live/live-1`。
  - 买家登录并出价。

## 当前优先落地结果

本轮已优先完成：

1. 一键恢复标准演示场景。
2. `npm run demo` 单命令启动。
3. 商品导入模板加图片列，并默认本地图片兜底。
4. `/api/demo/check` 演示健康检查接口。
5. 主播端基础数据看板升级。
